[package]
name = "geese"
version = "0.0.1"
edition = "2024"
license = "GPL-3.0-or-later"

[dependencies]
anyhow = "1.0"
clap = { version = "4.5", features = ["derive"] }
dirs = "6.0"
serde = { version = "1.0", features = ["derive"] }
thiserror = "2.0"
toml = "0.8"

[dev-dependencies]
assert_cmd = "2.0"
tempfile = "3.10"
